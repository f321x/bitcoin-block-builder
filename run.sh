# goes into solution rust directory and compiles and runs the code in release mode (with optimizations)

# install rust with curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

cd ./implementation && cargo run --release
